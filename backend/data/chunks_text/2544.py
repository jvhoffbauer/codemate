- Tests a WebSocket connection to the `"/prefix/"` endpoint using the Flask-Testing library's `TestClient`.
- Receives and verifies that the initial message sent by the server is "Hello, router with prefix!".