- Tests if user with token 'jessica' can access items endpoint using a fake super secret token as an authorization header
- Verifies that expected items (plumbus and portal gun) are returned in JSON format when request is successful