- This function is a FastAPI route with GET method and path `"/"`.
- It returns an HTML document as response using `response_class=HTMLResponse`, which allows us to set headers for content type, charset, etc.
- The returned string contains basic HTML structure with title and body elements.