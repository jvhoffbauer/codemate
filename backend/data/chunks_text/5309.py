- Defines a function `ColorMapParams` that takes an optional argument `colormap_name`.
- If `colormap_name` is provided, it retrieves the corresponding dictionary from the `cmap` dictionary using its value and returns it as an optional result.
- Otherwise, it returns `None`.