- Defines a Pydantic model `Item` with nested lists for `examples` and `openapi_examples`. - Uses FastAPI's `Body()` decorator to automatically convert JSON request body into an instance of `Item`. - Provides two types of examples: one using the `examples` list (example1) and another using the `openapi_examples` dictionary (example2). The latter is used by Swagger UI to display examples directly from the API documentation.