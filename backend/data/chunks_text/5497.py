- Tests the `GET /mosaicjson/info` and `GET /mosaicjson/info.geojson` endpoints for retrieving metadata about a Mosaic Tile Pack (MTP) using Flask's built-in testing framework. - Verifies that the expected HTTP status codes, JSON keys, and values are returned by both endpoints. - Demonstrates how to retrieve additional information such as quadkey coverage from an MTP using the GeoJSON format.