- Defines an asynchronous function `verify_token` that takes a string argument `x_token`, which is extracted from the request headers using FastAPI's built-in `Header()` decorator. - Checks whether the value of `x_token` matches a hardcoded secret token ("fake-super-secret-token"). If not, raises a customized HTTP exception with status code 400 and error message "X-Token header invalid".