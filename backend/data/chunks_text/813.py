- Defines a function `check_password` that takes two arguments: `password`, which is the plaintext user input, and `password_in_db`, which is the hashed password stored in the database. - Converts the plaintext password to bytes using UTF-8 encoding (line 2). - Calls the `bcrypt.checkpw()` method with the converted byte string as the first argument and the second argument being the hash value of the password from the database (line 4). - Returns True if the comparison succeeds; False otherwise (line 5).