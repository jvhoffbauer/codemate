- This function takes an optional `authorization_header_value` as input and returns a tuple containing the authorization scheme (e.g., "Bearer") and the parameter value (e.g., the access token). - If no authorization header is provided, it returns empty strings for both the scheme and parameter values. - The function splits the authorization header into its components using string partitioning with whitespace as the separator.