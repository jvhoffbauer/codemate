- Defines an asynchronous function `read_items` that takes a positional argument `x_token`, which is optionally annotated with the `Header()` decorator from Pydantic's `headers` module to parse it as a header value. - If `x_token` is not provided (i.e., `None`), returns a dictionary containing a key-value pair for any X-Token headers found in the request. - Otherwise, returns a dictionary containing a key-value pair for the specified X-Token header(s).