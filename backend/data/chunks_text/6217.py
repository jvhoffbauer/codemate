- Defines a function called `country_population` that takes two arguments: `country_code` and an optional argument `default`.
- If `country_code` is found in the dictionary `POPULATIONS`, returns its corresponding value (i.e., population). Otherwise, returns the value of `default`.
- Returns the population as an integer.