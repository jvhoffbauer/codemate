- Compares parameter signatures of FastAPI and Starlette's `Router` class methods to ensure consistency between both frameworks. - Uses Python's built-in `inspect` module to retrieve function signatures. - Asserts that annotations, defaults, and values are identical across corresponding parameters from all three functions (base API route, Router method, and FastAPI global method).