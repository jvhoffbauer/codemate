- `exists_global()` is a function that checks if a global variable exists based on its name and optional alias. - The function takes two arguments: `name`, which is required, and an optional `alias`. If no alias is provided, it defaults to `DEFAULT_ALIAS`. - The function returns `True` if either the tuple of `(name, alias)` or just `name` is present as a key in the dictionary `__globals__`. Alternatively, it returns `True` if the default alias has been used and the variable still exists using another helper function called `_exists_faa_global()`.