- `__post_init__` is a special method called after object initialization in Python classes.
- This method registers routes (URLs) for the class's functionality using the `register_routes()` function.
- It also configures any specific options required by those routes during this stage of initialization.