- This function is an asynchronous view function that takes a `Request` object and returns either an optional string (the user's authentication token) or raises an exception. - It extracts the Authorization header from the request and parses it to obtain the authentication scheme ("Bearer") and parameter (the actual token). - If no Authorization header is present or the wrong scheme is used, it can either raise a HTTPException with a WWW-Authenticate header for Bearer schemes or simply return None without raising any error (depending on a configuration flag).