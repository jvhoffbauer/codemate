- Defines a function `get_password_hash` that takes a password as an argument of type string. - Uses the `pwd_context` object to hash the given password using the default hashing algorithm (SHA256). - Returns the resulting password hash.