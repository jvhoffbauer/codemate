- Defines a dictionary called `LOGGING` with version 1 of logging configuration format. - Disables existing loggers to allow adding custom ones. - Creates two filters for correlation ID and Celery tracing IDs. - Specifies full formatting string that includes both correlation and Celery tracing IDs. - Adds console handler with specified filtering and formatting options. - Configures the 'asgi_correlation_id' logger with the created console handler, DEBUG level, and propagation enabled.