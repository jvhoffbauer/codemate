- Tests that Swagger UI is accessible by making a GET request to /docs and checking for a successful status code
- Verifies that overridden configuration values are preserved while unnecessary ones are excluded from the output
- Confirms that default configurations such as deep linking, syntax highlighting, DOM ID, presets, layout, show extensions, and show common extensions remain intact