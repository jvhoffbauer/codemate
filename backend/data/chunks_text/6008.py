- Tests different scenarios for setting and accessing `Settings.database_url` and `Settings.database_url_async`. - Verifies that `Settings.database_url` is set when a synchronous SQLAlchemy URL is provided, while `Settings.database_url_async` remains empty. - Confirms that both `Settings.database_url` and `Settings.database_url_async` are empty when an asynchronous SQLAlchemy URL with "sqlite+" prefix is used instead of "postgresql+" or "mysql+".