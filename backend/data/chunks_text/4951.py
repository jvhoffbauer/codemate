- Initializes a new instance of the class with optional arguments for `detail`, `status_code`, `biz_code`, and `errors`. - Sets default values for `status_code` and `code` based on the provided value or the current object's existing value. - Allows chaining by returning `self` at the end of each assignment statement. - Appends any provided list of `errors` to an internal list if not already set.