- Applies a multiplication factor to each pixel value in an input `ImageData`.
- Stores the resulting values as a new `numpy` array called `data`.
- Creates and returns a new `ImageData` object with the updated `data`, original mask, asset list, CRS, and bounding box information from the input `ImageData`.