- Defines a function `get_common_counter()` that takes an optional argument `common`.
- Initializes a global variable `_common_counter` and increments it inside the function using the `nonlocal` keyword.
- Returns a tuple containing the value of `common` and the updated value of `_common_counter`.