- Starts a live server using MkDocs' `serve` command with livereload enabled
- Allows specifying a language (defaulting to 'en') as an argument, which determines the directory containing the documentation to be served
- Only serves the actually translated documents, excluding placeholder pages generated by `build-all`.