- Defines an `ArticleAdmin` for Django's admin interface using Graphene-Django's `RegisterAdminMixin`.
- Sets up read and permission fields for different actions (view, update, create).
- Tests that the correct fields are included or excluded from various permissions lists.