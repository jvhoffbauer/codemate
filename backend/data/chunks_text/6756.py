- Decorates a method with type hinting using Pytest-Mako's `@ep.method()`.
- Defines input and output types for the decorated method using Python's built-in type annotations (`Input` and `Output`, respectively).
- Returns an empty dictionary from the decorated method.