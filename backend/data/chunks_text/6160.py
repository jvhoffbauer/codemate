- Defines a task called `docker` using Invoke framework that can be invoked with options for building, running, tagging, and naming Docker containers. - Raises an error if neither `--build` nor `--run` is specified to ensure user input. - Builds a new image (if `--build` option is provided), otherwise runs an existing one (specified by `--tag`) in interactive mode (-i flag). - Exposes port 80 of the container and assigns it a unique name generated randomly (using Python's random module).