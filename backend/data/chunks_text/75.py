- Defines an `exec()` method with an optional parameter for a SQL SELECT query (`statement`) and options to customize its behavior (e.g., parameters, execution settings).
- Returns a tuple containing the result of executing the query as defined by the `TupleResult` type alias.
- The implementation is not shown in this snippet but can be found inside the function body marked by ellipsis (...)