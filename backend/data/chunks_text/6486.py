- Adds a new method route to an existing endpoint using FastAPI's `add_method_route()` function.
- Allows specifying a custom name for the route (default is the function name).
- Extends the list of tags associated with the route based on user input.
- Creates a new instance of the `MethodRoute` class and adds it to the list of routes for this endpoint.