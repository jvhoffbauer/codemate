- Sends a GET request to the root URL (/) using Flask's built-in `client` object
- Assigns the response of this request to the variable `response`
- Asserts that the text content of the response is equal to the string `html`, which presumably contains the HTML for our web app's homepage