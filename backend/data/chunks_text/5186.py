- Defines three endpoints for cropping images based on different input formats and parameters. - Takes in various dependencies such as `geojson`, `format`, `src_path`, `layer_params`, etc., to generate an output image. - Applies optional transformations like rescaling, color formulas, and colormaps using `rescale`, `color_formula`, and `colormap`. - Returns the generated image in the specified format using `Response`.