- Defines a function `test_basic` that takes a parameter `json_request`.
- Makes two requests using the provided request handler (`json_request`) with different methods ("probe1" and "probe2").
- Asserts that both responses have the expected format and data.