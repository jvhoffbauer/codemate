- Establishes a database connection using SQLAlchemy's `Session` class and passes in our engine object created earlier. - Executes an SQL query to select all rows from the 'hero' table using SQLAlchemy's `select` function, passing in our `Hero` model class. - Retrieves the results of the query using the `all()` method provided by SQLAlchemy's `Query` object, which is returned by calling `exec()`. - Returns the list of retrieved hero objects.