- Defines a new endpoint `/third` using FastAPI's decorator syntax
- Accepts an argument of type `Item`, which is defined elsewhere in the application
- Returns the received `Item` object without any further processing or validation