- Retrieves a list of language codes from the `languages` directory within the `partials` subdirectory of the MkDocs Material template files.
- Uses Python's pathlib module to navigate through directories and extract filenames without extensions using list comprehension.
- Returns the resulting list of language codes as a value that can be used elsewhere in the program or application.