- Retrieves user information using FastAPI's dependency injection to obtain the authenticated user from the request context (`Depends(deps.get_current_user)`) and returns it as a JSON response with status code `200 OK`. - The route is annotated with OpenAPI documentation, including a descriptive summary, operation name, and a note that authentication is not required for this endpoint.