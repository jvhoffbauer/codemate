- Creates three new heroes using the `Hero` class and initializing their attributes in the constructor call. - Uses a context manager to open a database connection, adds the newly created heroes to the session, commits changes, and closes the connection automatically.