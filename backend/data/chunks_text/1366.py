- Defines a GET request for the `/items/` endpoint using FastAPI's decorator syntax (`@app.get`)
- Includes a query parameter named `strange_header`, which can be either a string or `None`. The `Header` function is used to parse and validate this header with custom options such as default value and case sensitivity.