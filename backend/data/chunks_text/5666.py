- Retrieves a synchronization database with the given alias (default is 'DEFAULT_ALIAS') using either global or site scope. - First checks for existence of the database in global scope and returns it if found. - If no global database is found, looks for an existing site with the same alias and retrieves its database object. - Raises an error if neither global nor site database could be found.