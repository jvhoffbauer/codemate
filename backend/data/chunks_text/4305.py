- Tests the Swagger UI endpoint by making an HTTP GET request and checking the status code and content of the response.
- Verifies that unused parameters are not included in the output, while parameters with middle dots are handled correctly as JSON objects.
- Confirms that default configuration options such as syntax highlighting theme, DOM ID, presets, layout, deep linking, and common extensions are properly applied or preserved.