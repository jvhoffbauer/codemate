- Endpoint to retrieve a list of items with FastAPI's `GET` request method
- Response model is defined as `List[Item]`, where `Item` is a custom data class representing each item in the list
- The endpoint returns an array containing two sample items, each represented by a dictionary with 'name' and 'price' keys