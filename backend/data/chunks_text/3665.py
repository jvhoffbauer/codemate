- Tests if an API request with a valid item name ("bar") and an invalid token results in a HTTP status code of 400 Bad Request and an error message containing "X-Token header invalid". - Uses FastAPI's built-in `TestClient` to simulate requests without starting a server instance. - Demonstrates how to pass custom headers (such as authentication tokens) using the `headers` parameter in the GET request.