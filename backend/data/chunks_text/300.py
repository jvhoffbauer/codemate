- Retrieves a list of heroes from the database using SQLAlchemy's `Session` and `select()` functions
- Filters the query to include only heroes whose age is greater than or equal to 35 and less than 40 using `where()` function
- Prints each selected hero object returned by the query