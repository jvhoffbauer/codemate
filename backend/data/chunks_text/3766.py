- Defines a fixture named `client` for use in pytests using Flask's `TestClient`.
- Imports the necessary module and class (`app`) to create an instance of `TestClient`.
- Returns the created `TestClient` object for use in tests.