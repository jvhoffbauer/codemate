- This function is a unit test using Pytest and FastAPI's built-in testing framework `TestClient`.
- It tests the GET request to the root URL ("/").
- The expected HTML content is passed as an argument (`html`) and compared with the actual response text returned by the server.