- Creates a new `Hero` object with name and secret name using SQLAlchemy's session management.
- Commits changes to the database.
- Sends a PATCH request to update the name of the hero using Flask-Restful's `TestClient`.
- Verifies that the status code is 200 (OK), the updated name is returned in the JSON body, the original secret name and age are unchanged, and the ID matches the one generated by SQLAlchemy.