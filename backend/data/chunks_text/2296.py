- Defines a function `query_convertor` that takes an optional parameter `param` of type `Query`. If not provided, defaults to None (not shown in example). - The returned value is a dictionary with a key "query" and its corresponding value being the converted `param`. This conversion is handled by the built-in Pydantic `Query` class which parses incoming URL parameters as Python objects.