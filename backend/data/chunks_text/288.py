1. Defines a function `create_db_and_tables()`.
2. Uses SQLAlchemy's `SQLModel` to define database models and metadata.
3. Creates all tables in the defined metadata using `create_all()` method of engine object created earlier.