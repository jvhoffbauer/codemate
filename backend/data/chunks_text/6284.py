- Sends a GET request to `http://localhost:8000/v2/latest` with optional query parameters using AsyncHTTPClient from FastAPI's built-in testing utilities. - Prints the URL and HTTP response details of the request. - Parses the JSON response body into a dictionary and prints it in pretty format (using pformat). - Asserts that the status code is 200 OK and checks specific values within the "latest" object returned by the API endpoint.