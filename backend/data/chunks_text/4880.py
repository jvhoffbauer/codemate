- Defines a function `ensure_create_full_text_indexes()` that creates Couchbase Full Text Search (FTS) indexes based on JSON definitions stored in a directory specified by `index_dir`. - Takes optional arguments for FTS credentials and connection details to connect to the Couchbase server at `localhost:8094`. - Iterates over all files ending with ".json" in the `index_dir`, loads their contents into an `index_definition` dictionary, extracts the required `name` field, retrieves any existing index with the same name using `get_index()`, and creates it if it doesn't exist already using `create_index()`. - Raises an AssertionError with a custom error message if the index creation fails or if the `name` field is missing from the index definition.