- Sends a PATCH request to update the name of an item with ID 'foo' using Flask's built-in `client` object
- Expects a successful HTTP status code (200) and checks that the expected JSON response is returned by the server


Based on the text material above, generate the response to the following quesion or instruction: How can you use Flask's `request` object to access data sent in a POST request body?