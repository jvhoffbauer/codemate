- Defines an overloaded function called `select` with three arguments: a scalar value (`entity_0`) and two arrays (`__ent1` and `__ent2`, marked as unused). - Returns a tuple containing the original scalar value, followed by elements from both input arrays that meet certain criteria specified elsewhere in the library's documentation.