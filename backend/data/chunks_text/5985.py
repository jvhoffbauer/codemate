- Tests whether `annotation_outer_type()` correctly identifies the outer type of a scalar enum and its optional variant using the built-in Python `Enum`.
- Uses the `assert` statement to verify that the returned types match the expected ones for both cases (plain enum and optional enum).