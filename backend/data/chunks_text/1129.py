- Defines an asynchronous function `get_current_user()` that takes a token parameter and uses it to retrieve the current user from the OAuth2 scheme (implemented by FastAPI's built-in dependency injection). - If the decoding of the token fails or returns None, raises a HTTPException with status code 401 Unauthorized and WWW-Authenticate header set to Bearer. - Otherwise, returns the retrieved user object.