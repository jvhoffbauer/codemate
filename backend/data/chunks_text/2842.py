- Tests if `multipart` package is not installed and raises a `RuntimeError` when trying to create an instance of `FastAPI`. - Uses `pytest.fixture` (`monkeypatch`) to replace the version attribute of `multipart` module temporarily during testing. - Raises `RuntimeError` with custom error message "Multipart middleware requires'multipart' >= 0.0.5" using regular expression matching in case `multipart` is not installed or its version is less than required by `FastAPI`.