- Defines a function `get_full_name` that takes two arguments `first_name` and `last_name`, both of type string
- Inside the function, concatenates the titleized versions of `first_name` and `last_name` using space as separator
- Returns the resulting full name