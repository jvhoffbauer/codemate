- Retrieves a list of User objects from the database using SQLAlchemy's query method
- Allows for pagination by accepting optional `skip` and `limit` arguments to offset and limit the number of returned results respectively
- Returns all matching User records as a list