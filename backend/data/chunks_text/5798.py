- Defines a method called `handle` that is asynchronous (async).
- Takes three arguments: `request`, `item_id`, and `data`. The type of `data` can be either `Optional[SchemaUpdateT]` or None.
- Returns an object of type `BaseApiOut` with a `data` attribute containing the updated data.