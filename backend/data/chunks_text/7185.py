- Generates a token with specified data (public information) and optional expiration time
- Uses Flask-JWT-Extended library for generating JWT tokens
- Follows best practices of storing sensitive data in environment variables instead of hardcoding them directly into the function